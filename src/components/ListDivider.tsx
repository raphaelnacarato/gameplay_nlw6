import React from 'react';
import { View } from 'react-native';

import { styles } from '../styles/components/ListDivider';

export function ListDivider() {

   return (
      <View style={styles.container} />
   );
};
